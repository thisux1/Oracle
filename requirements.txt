discord.py-self
tflite-runtime
numpy
Pillow
colorama
aiohttp
textual
fastapi
uvicorn[standard]
python-multipart
pywinpty; sys_platform == "win32"
# Build dependencies (install separately or use build_windows.py)
# pyinstaller
# node (for frontend build)
